# CV Lab Starter

Стартер для лабораторной работы: персональный CV → CI/CD → GitHub Pages.

> **TODO студент:** удали этот блок. Заполни README своим — про себя, про CV, и **обязательный раздел "Vibe coding log"** (см. внизу).

## Стек

- [Vite](https://vitejs.dev/) — dev server + бандлер
- Vanilla JS + HTML + CSS (можешь добавить TS / фреймворк — на свой страх и риск, CI/lint придётся подкрутить)
- ESLint 9 (flat config) — линтер
- GitHub Actions — CI + CD

## Локальный запуск

```bash
npm install
npm run dev      # dev server на http://localhost:5173
npm run lint     # проверить код
npm run build    # собрать в dist/
npm run preview  # посмотреть прод-сборку локально
```

## Структура

```
.
├── .github/workflows/
│   ├── ci.yml        # PR → lint + build
│   └── deploy.yml    # main → GitHub Pages
├── src/
│   └── main.js       # JS-точка входа
├── index.html        # CV здесь
├── style.css
├── eslint.config.js
├── vite.config.js
└── package.json
```

## Что делать

1. Форкни template или используй "Use this template".
2. Сделай репо публичным.
3. **Settings → Pages → Source: GitHub Actions** (важно, без этого деплой не пройдёт).
4. Заведи ветку `feature/my-cv`, перепиши `index.html` / `style.css` под свой CV.
5. Открой PR в `main`. Дождись зелёного CI.
6. Merge. Смотри Actions — должен запуститься deploy.
7. Открой `https://<username>.github.io/<repo>/` — твой CV в проде.

Подробнее — `STUDENT_GUIDE.md`.

## Vibe coding log

- **LLM-ассистент:** Qwen 3.6
- **2–3 ключевых промпта:**
  1. заготовка index.html с которой я работаю.(прикрепил html файл).Мне нужно чтобы ты, как хороший программист, но без невероятно тяжелых ходов поменял имя на Artem Skorohodov,в контактах указал мою почту (g.va1akas@yandex.ru) и номер телефона (+79990256040)
в one line summary переведи и вставь на английском: студент 1го курса СПБПУ по направлению математическое обеспечение и администрирование цифровых систем.В нестандартном элементе пока ничего не делай, предложи какие-нибудь варианты easter egg.
предложи свои идеи того, что будет в summary
в опытах/проекте вставь на английском, что я написал картотеку на cpp,также написал там калькулятор,база данных работников при помощи двусвязного списка, опыта работы на данный момент по специальности нет
в скиллах cpp,вайбкодинг,решение интегралов,умение по каноническим уравнениям определить вид кривой второго порядка, владение английским уровня intermediate
покажи мне каркас, доработаем вместе.

2.Отлично,в качестве easter egg хочу кнопку vibe mode и тему matrix,tagline hybrid (1st-year SPbPU student · C++ & math · Writing code that compiles on the first try (sometimes))
пока трогаем только html,css чуть позже.Есть варианты улучшить адаптивность прямо в html?

3.matrix theme ломают тему,дождь не добавлен,добавь настоящую анимацию,добавь плавные переходи между секциями при скролле, трек https://music.yandex.ru/album/40188837/track/147169317?utm_source=web&utm_medium=copy_link
добавь еще скример по кнопке, кнопка должна быть со страшным лицом

- **Что правил(а) руками после генерации:** формат моего номера казался мне корявым (+7-(999)-025-60-40).В контексте дизайна страницы выглядело непривлекательно,так что я переделал.Вставил ссылку на гитхаб,но самое главное,нейросеть почему-то сгаллюцинировала что я сделал калькулятор,который может считать конические фигуры,интегралы и всякие другие сложные вещи,хотя напрмяую я ему это не говорил.Видимо,так ему показалось,потому что в скиллах я указал нечто похожее.С остальными частями проекта он тоже выдумал,но в этот раз действительно попал в то,что я сделал,поэтому не менял.В целом мне кажется не нашлось бы ничего такого,что нейросеть не смогла бы сделать после моей подсказки,правил я что-то руками исключительно из соображений скорости (если видел и понимал,где код и на что влияет,я сразу переписывал,а не думал как написать промпт + ждал ответ от нейросети).

## Live URL

> **TODO студент:** вставь сюда https://artemartemof89-hash.github.io/lab6/
